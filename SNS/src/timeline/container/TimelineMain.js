import React from 'react';


function TimelineMain() {
  const [, forceUpdate] = useReducer(v => v+1,0);
  useEffect(() => {
    const unscribe = store.
    return () => {
      cleanup
    }
  }, [input])
  return (
    <div>
      
    </div>
  )
  
export default TimelineMain
